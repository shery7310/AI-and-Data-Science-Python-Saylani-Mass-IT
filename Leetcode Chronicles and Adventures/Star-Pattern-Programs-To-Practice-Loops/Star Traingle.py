"""
*
* *
* * *
* * * *
* * * * *
"""

for row in range(1, 6): # we need 6 rows
    print(('*' + " ") * row, end="\n") # after each * we add a space after it,
    # The * is printed times the row. if row is 2 the * is printed * times
    # after a pattern of * is printed in a row \n in end takes it to the next line

